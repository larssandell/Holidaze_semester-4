import RangePickerVali from '../RangePicker';

const RangePickerComp = ({ id, maxGuests }) => {
    return (
        <div>
            <RangePickerVali id={id} maxGuests={maxGuests} />
        </div>
    );
};
export default RangePickerComp;
